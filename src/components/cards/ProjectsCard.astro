---
import type { ProjectItem } from "../../types/project";
const props = Astro.props as ProjectItem;
import { Image } from "astro:assets";
---

<article data-cuelume-hover="press" class="h-full hover:bg-amber-50 duration-200 transition bg-white">
    <a href={`/projects/${props.content_link}`} class="-mr-px -mb-px -mt-px flex h-full min-h-72 flex-col gap-5 border px-5 py-8 transition hover:shadow-sm focus:outline-none focus-visible:ring-2 focus-visible:ring-black/40">
        <div class="w-fit rounded-full self-end border px-3 py-1 text-xs">{props.about}</div>

        {props.image ? (
          <Image
            class="h-60 w-full object-cover border border-neutral-200 bg-black/60"
            src={props.image}
            alt={props.name}
            loading={props.priority ? "eager" : "lazy"}
            fetchpriority={props.priority ? "high" : undefined}
            decoding={props.priority ? "sync" : "async"}
          />
        ) : (
          <div class="h-60 w-full bg-black/60" aria-hidden="true"></div>
        )}
        <div class="flex flex-col">
            <h3 class="text-base font-medium">{props.name}</h3>
            <p class="text-sm text-zinc-700">{props.description}</p>
        </div>
    </a>
</article>
